Instructions for NDN-FCH HTML Server:

Assumptions:
- geoip2, kdtree, json, cPickle, pycurl packages installed

Download http://geolite.maxmind.com/download/geoip/database/GeoLite2-City.mmdb.gz and unzip the file GeoLite2-City.mmdb to the local directory.

Run ndn_fch_server.py in the background.

For instance, "sudo nohup python /PATH/TO/FILE/ndn_fch_server.py &"
